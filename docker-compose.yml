services:
  screenlog:
    build: .
    # 같은 네트워크(LAN)의 다른 기기도 접속 가능하도록 모든 인터페이스에 바인딩.
    ports:
      - "8000:8000"
    volumes:
      - ~/.screenpipe/db.sqlite:/root/.screenpipe/db.sqlite:ro
      - ./chroma:/app/chroma
    env_file:
      - .env
    restart: unless-stopped
